import 'package:flutter/material.dart';

class AppStyle{
        TextStyle fontSize(double size,Color color,dynamic fontWeight)=>TextStyle(
            color:color,
            fontSize:size,
            fontWeight:fontWeight,
        );

}